{# <table class="table table-borderless">
	<tbody>
		<tr>
			{% for dish in dishes %}
			<td>
				<div class="row">
					<a href="{{ path('dish_single', {id: dish.id}) }}">
						<img class="img-fluid img-thumbnail" src="{{ asset('images/' ~ dish.image) }}" alt="{{ dish.name }}" style="height: 120px; width: 90%" />
					</a>
				</div>
				<div class="row">
					<a href="{{ path('dish_single', {id: dish.id}) }}">
						<h6>{{ dish.name }}</h6>
					</a>
				</div>
				<div class="row">
					<a href="{{ path('dish_single', {id: dish.id}) }}">
						<p>{{ dish.createdOn|date }}</p>
					</a>
				</div>
			</td>
			{% endfor %}
		</tr>
	</tbody>
</table> #}


<div class="row">
{% for dish in dishes %}
	<div class="col-4">
		<div class="row">
			<a href="{{ path('dish_single', {id: dish.id}) }}">
				<img class="img-fluid img-thumbnail" src="{{ asset('images/' ~ dish.image) }}" alt="{{ dish.name }}" style="height: 120px; width: 90%" />
			</a>
		</div>
		<div class="row">
			<a href="{{ path('dish_single', {id: dish.id}) }}">
				<h6>{{ dish.name }}</h6>
			</a>
		</div>
		<div class="row text-center">
			<a href="{{ path('dish_single', {id: dish.id}) }}">
				<p>{{ dish.createdOn|date }}</p>
			</a>
		</div>
	</div>
{% endfor %}
</div>